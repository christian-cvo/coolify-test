version: '3.8'

services:
  web:
    build: .
    container_name: coolify-website
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    # Für Coolify: Uncomment die nächste Zeile für automatische Restarts
    # restart: always
